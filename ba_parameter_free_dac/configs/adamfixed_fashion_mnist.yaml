defaults:
  - base

name: Adam_fixed_fmnist
agent:
  type: StaticAgent
  lr: 0.001
num_episodes: 1
optimizer_type: adam

dacbench_sgd_config:
  cutoff: 30
  dataset_name: FashionMNIST
  fraction_of_dataset: 1.0