defaults:
  - cluster/cpu

hydra:
  run:
    dir: results/${name}_${dacbench_sgd_config.dataset_name}/${seed}
  sweep:
    dir: results/${name}_${dacbench_sgd_config.dataset_name}/${seed}
  job:
    chdir: true

name: base
seed: 0
dacbench_sgd_config:
  dataset_name: dataset
